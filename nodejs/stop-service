#!/bin/perl
use strict;
use warnings;
use Cwd qw(abs_path);
my $cwd = abs_path(getcwd());
my $pidfile = "$cwd/service.pid";
open(my $fh, "<", $pidfile) || die("Could not open file: $pidfile $!");
while(<$fh>) {
    chomp $_;
    system("kill $_");
}
close($fh);
unlink $pidfile;
